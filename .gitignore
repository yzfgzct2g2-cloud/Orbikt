node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
coverage
.vite
node_modules/.tmp

# Never commit credentials or service-account keys
serviceAccountKey.json
**/serviceAccountKey.json

# source-systems/ are LOCAL reference copies only (their own git repos, node_modules,
# dist, runtime output, and PII). They must never be committed into the Orbikt repo.
# See docs/SOURCE_SYSTEMS.md.
source-systems/

# Raw source files (PII) — import-time only, NEVER commit
input/
mock-data/
.claude/heartbeat.ps1
